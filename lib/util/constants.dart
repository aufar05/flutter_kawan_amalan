import 'package:flutter/material.dart';

const Color loginBg = Color(0xFF00C470);
const Color signupBg = Color(0xff203E58);

const double defaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 300);
